<h1>Editing user</h1>

<%= form_for(@user) do |f | %>
	<%= render 'fields',  :f => f %>
	<div class="actions">
		<%= f.submit  'Update' %>
	</div>
<% end %>
<div>
	<%= gravatar_for @user %>
	<a href="http://gravatar.com/emails">change</a>
</div>
<%= link_to 'Show', @user %> |
<%= link_to 'Back', users_path %>
